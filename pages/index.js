export default function Home() {
  return (
    <div style={{ fontFamily: "sans-serif", padding: "20px" }}>
      <h1>Redirect API</h1>
      <p>
        Sử dụng endpoint <code>/api/redirect?url=https://example.com</code> để
        chuyển hướng.
      </p>
    </div>
  );
}
