{
  "version": 2,
  "builds": [{ "src": "package.json", "use": "@vercel/next" }],
  "outputDirectory": ".next",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
